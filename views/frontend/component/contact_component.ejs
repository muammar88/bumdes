<h4 class="title-footer mr-auto">Hubungi Kami</h4>
<ul class="list-group">
     <% for ( let index = 0; index < Object.keys(contact).length; index ++ ) { %>
          <li class="list-group-item d-flex  border-0 px-0">
          <i class="fas fa-phone pt-1 mr-2"></i>
          <%= contact[index].contact %>
          </li>
     <% } %>
     <li class="list-group-item d-flex  border-0 px-0">
          <img id="imagebrandheader" src="static/img/logo.png" alt="">
     </li>
</ul>